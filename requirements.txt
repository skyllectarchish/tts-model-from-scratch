# ──────────────────────────────────────────────
# Gujarati FastSpeech 2 TTS — Python Requirements
# Install: pip install -r requirements.txt
#
# For GPU training (recommended):
# Install PyTorch nightly (supports CUDA 12.8+ / Blackwell)
# pip3 install --pre torch torchaudio --index-url https://download.pytorch.org/whl/nightly/cu128
# For CPU only:
#   pip install torch torchvision torchaudio
# ──────────────────────────────────────────────

# Core deep learning
# (install PyTorch separately via the official wheel command above)
# torch>=2.1.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0

# Numerical / data
numpy>=1.24.0

# TensorBoard logging (used in train.py via torch.utils.tensorboard)
tensorboard>=2.14.0

# Progress bars
tqdm>=4.65.0

# ──────────────────────────────────────────────
# Optional — useful for data preparation & analysis
# ──────────────────────────────────────────────
pandas>=2.0.0
matplotlib>=3.7.0
scipy>=1.11.0